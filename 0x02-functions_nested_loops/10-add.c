#include "main.h"

/**
 * add - function that adds two ints and returns the result
 *
 * @i: take the input
 * @j: take the input
 *
 * Return: return the result
 */

int add(int i, int j)
{
	int k = 0;

	return (k = i + j);
}
